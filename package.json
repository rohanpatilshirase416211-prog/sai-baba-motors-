{
  "name": "saibaba-motors",
  "version": "1.0.0",
  "description": "साईबाबा मोटर्स — Used Cars & Bikes Platform",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"blue.bold,magenta.bold\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "seed": "npm run seed --prefix server",
    "build": "npm --prefix client install --include=dev && npm --prefix client run build && node -e \"const fs = require('fs'); if(fs.existsSync('client/dist')){fs.cpSync('client/dist', 'dist', {recursive: true});}\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
